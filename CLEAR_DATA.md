# 清除和重新测试指南

## 1. 清除本地存储数据
在微信开发者工具中：
1. 点击「普通编译」->「添加编译模式」
2. 选择「自定义条件」，在「启动页面」选择 `app`
3. 编译后，在模拟器中点击「设置」->「清除缓存」->「清除本地存储」

## 2. 清除云数据库数据
通过微信开发者工具的云开发控制台：
1. 点击左侧「云开发」
2. 进入「数据库」标签
3. 删除以下集合中的所有数据（删除前请确认是否需要备份）：
   - `userPlaylists` (歌单集合)
   - `performances` (演出集合) 
   - `pointRecords` (点歌记录集合)
   - `qrCodes` (二维码缓存集合)

## 3. 重新部署云函数
确保所有云函数已正确部署到线上环境：
1. 右键点击 `cloudfunctions` 文件夹
2. 点击「上传并部署：云端安装依赖（不上传node_modules）」
3. 确保 `api` 云函数已更新到最新版本

## 4. 重新测试
1. 重新编译小程序
2. 登录并创建新歌单
3. 检查新歌单的ID是否为16位
4. 测试二维码生成和扫描功能

## 验证方法
创建歌单后，在开发者工具的调试面板中查看：
```javascript
// 在控制台输入以下代码检查歌单ID
wx.cloud.callFunction({
  name: 'api',
  data: {
    action: 'getPlaylists',
    data: { userId: YOUR_USER_ID }
  },
  success: res => {
    console.log('歌单列表:', res.result.data)
  },
  fail: console.error
})
```

## 常见问题排查

### 问题1：ID仍然是32位
- 确认云函数是否正确部署
- 检查是否清除了云数据库中的旧歌单数据
- 确认是否在使用最新版本的小程序（非体验版）

### 问题2：二维码无法识别
- 检查生成二维码时的场景参数是否正确
- 确认扫描时是否正确解析了场景参数
- 验证数据库查询是否能找到对应的歌单