<!--components/icon/icon.wxml-->
<view
  class="icon-wrapper"
  style="width: {{size}}; height: {{size}};"
>
  <image
    class="icon"
    src="{{iconPath}}"
    style="width: 100%; height: 100%; {{color ? 'filter: drop-shadow(0 0 0 ' + color + ');' : ''}}"
    mode="aspectFit"
  />
</view>
